# Issue #5: 画像エンコーディングモジュールの実装

## 概要
画像を読み込み、CLIPモデルでベクトル化するモジュールを実装する。

## タスク
- [ ] `src/image_encoder.py` の作成
- [ ] ImageEncoderクラスの実装
  - 画像の読み込み（PIL）
  - 画像の前処理（リサイズ、正規化）
  - ベクトル化処理
  - バッチ処理対応
- [ ] サポート画像形式の検証（JPG, PNG）
- [ ] エラーハンドリング（ファイルが存在しない、形式が不正など）
- [ ] ユニットテストの作成

## 実装例
```python
from PIL import Image
import torch

class ImageEncoder:
    def __init__(self, clip_model):
        self.model = clip_model
    
    def encode_image(self, image_path):
        image = Image.open(image_path)
        inputs = self.model.processor(images=image, return_tensors="pt")
        with torch.no_grad():
            features = self.model.model.get_image_features(**inputs)
        return features.cpu().numpy()
```

## 実装結果

### 作成したファイル
- `src/image_encoder.py`: ImageEncoderクラス実装（単一画像・バッチ処理対応）
- `tests/test_image_encoder.py`: ユニットテスト（14件）

### テスト結果
すべてのテスト（14件）が成功:
- 単一画像のエンコード
- バッチ処理（画像数に応じたベクトル結合）
- エラーハンドリング（ファイルなし、不正形式、破損画像）
- サポート形式（JPG, PNG）
- RGBA画像のRGB変換

実行時間: 66.26秒

## ステータス
**完了** - 2026-02-10
