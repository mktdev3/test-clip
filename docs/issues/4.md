# Issue #4: CLIPモデルラッパーの実装

## 概要
rinna/japanese-clip-vit-b-16モデルを読み込むラッパークラスを実装する。

## タスク
- [ ] `src/clip_model.py` の作成
- [ ] CLIPModelクラスの実装
  - モデルの初期化
  - デバイス設定（CPU/GPU）
  - モデルとプロセッサーの読み込み
- [ ] 環境変数からの設定読み込み（`.env`）
- [ ] エラーハンドリングの実装
- [ ] ユニットテストの作成

## 実装例
```python
from transformers import AutoModel, AutoTokenizer, AutoImageProcessor

class CLIPModel:
    def __init__(self, model_name, device='cpu'):
        self.model = AutoModel.from_pretrained(model_name)
        self.tokenizer = AutoTokenizer.from_pretrained(model_name)
        self.processor = AutoImageProcessor.from_pretrained(model_name)
        self.device = device
        self.model.to(device)
```

## 成功基準
- モデルが正常に読み込まれる
- CPU/GPU両方で動作する
- テストが通る

## 優先度
高

## Phase
Phase 1: プロトタイプ開発

## 依存関係
- Issue #2 (依存パッケージのインストール)
- Issue #3 (ディレクトリ構成の作成)

## 推定工数
2時間
