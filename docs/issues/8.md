# Issue #8: 検索ロジックモジュールの実装

## 概要
テキストクエリに対してコサイン類似度で画像を検索するモジュールを実装する。

## タスク
- [x] `src/search.py` の作成
- [x] SearchEngineクラスの実装
  - コサイン類似度の計算（scikit-learn使用）
  - Top-K件の画像取得
  - スコアの正規化
- [x] 検索結果のフォーマット
- [x] エラーハンドリング
- [x] ユニットテストの作成

## 実装例
```python
from sklearn.metrics.pairwise import cosine_similarity
import numpy as np

class SearchEngine:
    def __init__(self, vector_store):
        self.vector_store = vector_store
        self.vectors, self.metadata = vector_store.load()
    
    def search(self, query_vector, top_k=10):
        similarities = cosine_similarity(query_vector, self.vectors)[0]
        top_indices = np.argsort(similarities)[::-1][:top_k]
        
        results = []
        for idx in top_indices:
            results.append({
                'image_path': self.metadata[idx]['path'],
                'score': float(similarities[idx])
            })
        return results
```

## 成功基準
- 検索が正常に動作する
- スコアが降順でソートされる
- Top-K件が正しく返される

## 優先度
高

## Phase
Phase 1: プロトタイプ開発

## 依存関係
- Issue #7 (ベクトルストレージモジュールの実装)

## 推定工数
2時間
